#include "holberton.h"
/**
 * _abs - Entry point
 * This is a function for print absolute value of a number
 * @n: is the variable
 * Return: int value
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
