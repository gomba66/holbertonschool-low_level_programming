#include "sort.h"
/**
 * selection_sort - Function that sorts an array of integer in
 * ascending order using the selection algorithm.
 * @array: Array.
 * @size: Size of the array.
 */
void selection_sort(int *array, size_t size)
{
	(void)array;
	(void)size;
}
