#include <stdio.h>
/**
 * main - Entry point
 * @argc: number of commands
 * @argv: other argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", (argc - 1));
	return (0);
	(void)argv;
}
